# 贡献与开发规范

> 本项目（量化交易零基础实战教程）的开发规范。改代码前花 3 分钟读一遍，
> 能省掉之后一小时的返工。

## 1. 环境与基础约束

- Python ≥ 3.10；改完代码必须让 **全部测试通过**：
  ```bash
  ./.venv/Scripts/python.exe -m pytest code/tests/ -q     # Windows
  ./.venv/bin/python -m pytest code/tests/ -q             # Mac/Linux
  ```
- **`.env`、`data/`、`.venv/`、`docs/reviews/` 永不入库**（`.gitignore` 已挡，别删规则）。
  密钥只能放 `.env`；代码里通过 `common/config.py` 读取，禁止硬编码。
- 所有脚本在**项目根目录**运行；模块间 import 走 `sys.path.insert(0, code/)` 既有模式。

## 2. 分支与提交

- `main` 是唯一主干，保持**随时可运行**（测试绿 + 回测可复现）。
- 提交信息用约定式提交（Conventional Commits），格式：

  ```
  <类型>(<范围>): <一句话中文描述，≤50 字>
  ```

  | 类型 | 用途 |
  |------|------|
  | feat | 新功能（新策略/新章节/新指标） |
  | fix  | bug 修复（正文写清根因与回归测试名） |
  | docs | 教程/README 纯文档改动 |
  | test | 只加/改测试 |
  | refactor | 不改行为的重构 |
  | chore | 配置/构建/依赖等杂项 |

  示例：`fix(live_bot): 止损检查提前到信号NaN早退之前，防止持仓裸奔`
- 一次提交做一件事。**修行为 bug 的提交必须带一个能抓住该 bug 的测试**（教训：
  实盘止损曾因 main 未接线而失效，测试直接调底层函数所以全绿——这类断线靠
  "从入口路径测"的疫苗测试防住，见 `code/tests/test_live_bot.py`）。

## 3. 代码规范

- 中文注释；注释写"为什么"，不写"这一行在干什么"。
- 公开函数必须有 docstring，写清参数语义与边界行为（尤其 NaN/空输入）。
- 目标仓位契约（全项目一致，改动需同步三处）：**NaN=维持现状不动，>0=持有态，0=清仓**。
  涉及 `engine.py`、`live_bot.py`、`strategy.py` 的改动必须跑分数仓位相关测试。
- 免费数据源的调用必须有：失败重试 + 本地缓存兜底 + 请求间隔 ≥1s。
- 实盘相关代码（`ch07_binance/`）的新分支逻辑：
  - 下单开关只认交易所实例真实沙箱态（`resolve_mode`），绝不读配置旗标；
  - 持仓只认机器人账本（`bot_base`），绝不按全所余额平仓；
  - 永久性错误（鉴权/订单被拒）停机+推送，瞬时错误退避重试。
- 涉及钱的数学先写测试再写实现（参照 `test_engine.py` 的"已知答案"式断言）。

## 4. 文档同步（教程项目的特殊性）

改了行为就必须同步文档，防"文档承诺 ≠ 代码行为"（本项目发生过，第 9 章曾让读者
跑一个不存在的 `--stop-loss` 参数）：

- CLI 参数/`.env` 键/输出格式变了 → 同步对应章节 + `.env.example` + `solutions/`；
- 章节号与代码目录号解耦（`ch07_binance` 是第 9 章）——引用章节用文档文件名搜索确认；
- 回测数字会随行情漂移：文中示例数字必须带"截至 YYYY-MM 快照"标注；
- 每章顶部导航链接保持完整（`← 上一章 | 第 N 章 | 下一章 →`）。

## 5. 提交前自检清单

```
[ ] pytest 全绿（72+ 用例）
[ ] 涉及回测行为的改动：510300 回测输出与改前对照过（有意变化需在提交说明里解释）
[ ] 涉及实盘的改动：dry-run 冒烟过
[ ] 文档与代码一致（CLI 参数、.env 键、章节引用）
[ ] git status 无意外文件（.env/data 绝不出现）
```

## 6. 发布代理与推送

- 本仓库已配置仓库级代理（`git config http.proxy`，127.0.0.1:7897）；
  代理未开时推送会失败，`git config --unset http.proxy && git config --unset https.proxy` 可撤。
