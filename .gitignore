# 秘密与本地文件，绝不提交
.env
data/
.venv/
__pycache__/
*.pyc
.pytest_cache/

# 内部评审工件（不面向读者）
docs/reviews/

# AI 工具内部工件
.zcode/

# Node（commitlint 本地依赖）
node_modules/

# PAT 缺 workflow scope，gate.yml 暂缓入库（补权限后 git add 恢复）
.github/workflows/gate.yml
