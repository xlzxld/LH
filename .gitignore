# 秘密与本地文件，绝不提交
.env
data/
.venv/
__pycache__/
*.pyc
.pytest_cache/

# 内部评审工件（不面向读者）
docs/reviews/

# AI 工具内部工件
.zcode/

# Node（commitlint 本地依赖）
node_modules/

# 本地 Agent 工作区与同步器机器记录（不属于项目内容）
.workbuddy/
.agents/.source
