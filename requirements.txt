# 核心依赖（一条命令装完即跑通前 6 章的全部功能）
pandas>=2.2,<4
numpy>=1.26
matplotlib>=3.8
requests>=2.31
python-dotenv>=1.0

# 加密货币交易所统一接口（第 9 章需要）
ccxt>=4.3

# 测试
pytest>=8.0

# ---- 可选：A股数据社区接口 akshare（第 3 章教学用）----
# 依赖树较大，是新手 pip 安装失败的头号嫌疑，因此默认不装（项目功能不受影响，
# A股数据默认走东方财富公开接口）。想跟第 3 章的 akshare 小节动手时手动装：
#   pip install akshare
# akshare>=1.14
